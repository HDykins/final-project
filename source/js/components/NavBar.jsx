var React = require('react');

var NavBar = React.createClass({
  render: function () {
    return (
      <div>
        <button>A nav button</button>
        <button>A nav button</button>
        <button>A nav button</button>
      </div>
    );
  }
});

module.exports = NavBar;
